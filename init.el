;; -*- lexical-binding: t; -*-

(doom! :input

       :completion
       company
       vertico

       :ui
       doom
       modeline
       ophints
       (popup +defaults)
       vc-gutter
       vi-tilde-fringe
       workspaces

       :editor
       (evil +everywhere)

       :emacs
       undo
       vc

       :term

       :checkers

       :tools
       lookup
       docke r

       :os

       :lang
       emacs-lisp
       json
       markdown
       nim
       sh
       yaml

       :email

       :app

       :config
       (default +bindings +smartparens))
